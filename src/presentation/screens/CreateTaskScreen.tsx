import * as React from 'react'
import { View, Text } from 'react-native'

const CreateTaskScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>CreateTask Screen</Text>
    </View>
  )
}

export default CreateTaskScreen
